Signals

export hello="ls -l"   (if job[0] == export etc etc etc)
	free line in lexer
test with "" as commands (cat) (command not found, just a "")

test output (will be fixed with pipe logic if we want)
test append output
cat kasdkasdkads | cat kasdkadskasdkdas || exit			(fix output)
ls | grep s > out | wc < out | cat
""|""
test|cat> (very bad)
work_minishell$ cd ../../../../../../.. (very bad, update prompt)
echo test|>cat
exit codes
export $var
$var >
echo Good luck > $var

TESTER DA MARGARIDA

fix echo -> echo hello "" world








tests:
	cd folder && rm -rf ../folder
	env -i
	unset PATH (same fix as pipex)
	fix leaks in normal cases (no errors)
	fix leaks in error cases
	fix leaks in NULL cases?
test every malloc functions with NULLS and see if everything is freed and protected?
# define malloc(x) NULL?


norminette

switch things to getenv instead of ft_getenv?
improve echo
rename variables
add colors to prompt
change SHELL=binzsh to shell=minishell
messages.h




export aaa=aaa bbb=bbb ccc=ccc ddd=ddd eee=eee fff=fff ggg=ggg hhh=hhh jjj=jjj